"""Private/local bot modules.

This package exists so you can place personal bots in:
`src/alertbot/bots/private/*.py`

Files in this directory are ignored by git by default (see `.gitignore` in the
same folder), but can still be imported locally by the controller.
"""

